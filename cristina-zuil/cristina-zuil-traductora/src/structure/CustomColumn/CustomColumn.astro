---
import './CustomColumn.css';

interface Props {
  sm?: string;
  md?: string;
  lg?: string;
  'sm-push'?: string;
  'md-push'?: string;
  'lg-push'?: string;
  class?: string;
  id?: string;
}

const { 
  sm, 
  md, 
  lg, 
  'sm-push': smPush,
  'md-push': mdPush,
  'lg-push': lgPush,
  class: className,
  id 
} = Astro.props;
---

<custom-column 
  sm={sm}
  md={md}
  lg={lg}
  sm-push={smPush}
  md-push={mdPush}
  lg-push={lgPush}
  class={className}
  id={id}
>
  <slot />
</custom-column>