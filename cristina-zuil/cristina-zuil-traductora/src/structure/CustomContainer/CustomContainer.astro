---
import './CustomContainer.css';

interface Props {
  full?: boolean;
  class?: string;
  id?: string;
}

const { full, class: className, id } = Astro.props;
---

<custom-container class={className} id={id} data-full={full ? 'true' : undefined }>
  <slot />
</custom-container>